import React from "react";
import { Navbar } from "../Navbar/Navbar";
import Logo from "../Logo/Logo";

export const Header = () => {
  return (
    <header>
      <Navbar />
      <Logo />
    </header>
  );
};
